@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    var user = await UserManager.GetUserAsync(User);
    <div class="dropdown">
        <a class="header-action dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-circle"></i><span class="d-none d-lg-inline"> @(user?.FullName ?? "Tài khoản")</span>
        </a>
        <ul class="dropdown-menu dropdown-menu-end">
            @if (User.IsInRole("Admin") || User.IsInRole("Employee"))
            {
                <li><a class="dropdown-item" asp-area="Admin" asp-controller="Dashboard" asp-action="Index"><i class="bi bi-speedometer2"></i> Trang quản trị</a></li>
                <li><hr class="dropdown-divider" /></li>
            }
            <li><a class="dropdown-item" asp-area="" asp-controller="Customer" asp-action="Profile"><i class="bi bi-person"></i> Hồ sơ của tôi</a></li>
            <li><a class="dropdown-item" asp-area="" asp-controller="Customer" asp-action="Orders"><i class="bi bi-receipt"></i> Đơn hàng của tôi</a></li>
            <li><a class="dropdown-item" asp-area="" asp-controller="Customer" asp-action="Addresses"><i class="bi bi-geo-alt"></i> Sổ địa chỉ</a></li>
            <li><a class="dropdown-item" asp-area="" asp-controller="Customer" asp-action="Wishlist"><i class="bi bi-heart"></i> Yêu thích</a></li>
            <li><hr class="dropdown-divider" /></li>
            <li>
                <form asp-area="" asp-controller="Account" asp-action="Logout" method="post">
                    <button type="submit" class="dropdown-item"><i class="bi bi-box-arrow-right"></i> Đăng xuất</button>
                </form>
            </li>
        </ul>
    </div>
}
else
{
    <a class="header-action" asp-area="" asp-controller="Account" asp-action="Login"><i class="bi bi-person"></i><span class="d-none d-lg-inline"> Đăng nhập</span></a>
}
